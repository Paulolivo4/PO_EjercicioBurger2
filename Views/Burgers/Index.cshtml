@model IEnumerable<PO_EjercicioBurger2.Models.Burger>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BurgerName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WithCheese)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PriceCeiling)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BurgerName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WithCheese)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PriceCeiling)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BurgerId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BurgerId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BurgerId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
